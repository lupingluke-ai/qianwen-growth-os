# 千问计划 · 多角色用户流程测试报告

> **修复状态（2026-07-25）：下列 13 个问题已全部修复并通过浏览器回归验证**，修复明细见文末「四、修复记录」。

- 测试时间：2026-07-25
- 测试方式：浏览器实机全流程操作（localhost:3000，dev server + PGlite 本地库）
- 覆盖角色：成员（周宁）、评审人（陈墨）、管理员（林晓）、游客（未登录）
- 覆盖流程：登录/退出、证据添加 ×3、晋级申请提交、评审通过、晋级后爬坡、团队分析下钻、成果库、管理设置（十级体系/成员）、评审中心三种范围

---

## 一、验证通过的核心链路

| 链路 | 结果 |
|---|---|
| 成员：3 条证据逐条添加 → 3/3 触发「可提交晋级申请」CTA → 选主评人 → 提交 → 状态锁定「评审中」 | 通过 |
| 评审人：待评 badge(1) → 详情（标准-证据映射完整）→ 填反馈 → 已通过 → 待评清零 | 通过 |
| 晋级闭环：周宁 L3 通过后自动变「当前 L4 · 下一级 L5」，通关清单切 L5，主评反馈正确回显 | 通过（逐层爬坡机制生效） |
| 团队分析：KPI/层级分布/小组对比/评审动态/晋级名单实时更新；晋级名单点击打开成员档案 | 通过 |
| 管理设置：十级体系编辑（无草稿禁用发布）、成员管理（无改动禁用保存）、角色统计 | 通过 |
| 权限：成员导航无「管理设置」与「全部评审」；评审人默认落「我的待评」 | 通过 |

---

## 二、问题清单

### P1 · 功能缺陷（建议优先修复）

**1. 「审核中」状态的成果永远无法被审核**
- 现象：成果库中王璐的「政策条款智能比对」状态为「审核中」，管理员视图既无「发布/退回」按钮，顶部统计也显示「待审核 0」，与该成果状态矛盾。
- 根因：`AssetRow` 与批量操作只识别 `reviewStatus === "待审核"`，统计同样只数「待审核」；数据中存在「审核中」这一未纳入流转的状态值。
- 影响：该成果卡死，无法发布也无法退回。
- 建议：统一成果状态枚举（待审核/已发布/待补充），将存量「审核中」迁移为「待审核」，或在 UI/后端同时兼容。

**2. 旧「目标机制」遗留的跨级在途申请与逐层爬坡规则冲突**
- 现象：全部评审中存在「林晓 L4 → L6（已提交）」的跨级申请。
- 根因：该申请产生于旧目标机制；`reviewDecision` 通过时直接 `current_level = review.targetLevel`，无逐级校验。
- 影响：一旦通过，林晓从 L4 直接跳到 L6，违背「一层层爬坡」新规则。
- 建议：对在途申请做一次数据迁移（target_level 收敛为 from_level+1），并在 reviewDecision 中增加 `targetLevel === fromLevel + 1` 的防御校验。

**3. Seed 演示数据与业务约束矛盾**
- 现象：①陈墨/王璐的在途申请「0 条证据」，而产品规则要求至少 1 条证据才能提交；②陈墨自己「评审中」但目标层证据 0/3、林晓「已提交」但 0/3。
- 影响：演示环境讲不通业务故事，也让「评审状态」与「证据进度」互相打架，容易误导演示与验收。
- 建议：修正 seed，使每条在途申请至少有 1 条对应层级证据。

### P2 · 交互与逻辑问题

**4. 有进行中申请时通关清单仍提示「还需 N 条证据」**
- 现象：陈墨 L7 证据 0/3 且申请「评审中」，清单底部显示「还需 3 条证据覆盖通关标准」，而非「晋级申请评审中」。
- 根因：`pendingReviewId` 分支只在 3/3 覆盖时才渲染，分支优先级颠倒。
- 建议：`pendingReviewId` 存在时优先显示「评审中」状态条。

**5. 游客首页语义矛盾**
- 现象：未登录显示「当前 L4.5（团队中位）· 下一级 L1 · 从真实工作结果开始」，中位数与「下一级 L1」并列自相矛盾；进度条按 4.5 点亮 reached 节点也似是而非。
- 建议：游客态隐藏「下一级」与进度点亮，只展示团队中位数与体系介绍。

**6. 点击头像立即退出登录，无确认**
- 现象：右上角账号 chip 单击即触发 signOut，无二次确认，极易误触。
- 建议：改为下拉菜单（个人信息/退出）或增加确认。

**7. 晋级弹窗步骤指示器是装饰性的**
- 现象：「1.确认层级 / 2.选主评人 / 3.确认提交」三步指示与实际单屏流程不对应（选完评审人即可直接提交）。
- 建议：删除步骤条或做成真实分步。

**8. 「评审动态」下钻口径不一致（管理员）**
- 现象：团队分析-评审动态卡片统计的是全局（已提交1/评审中2/待补证2），点击跳转评审中心后落在「我的待评 4」（个人视角），数字对不上。
- 建议：管理员点击评审动态时落「全部评审」scope。

### P3 · 文案与一致性

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| 9 | 登录页 | 入口叫「管理员密码登录 →」，但成员/评审人也走此入口 | 改为「账号密码登录」 |
| 10 | 管理设置-添加成员 | 提示「自动进入 3 步引导」，实际引导已改为 2 步 | 改「2 步引导」 |
| 11 | 证据弹窗 | 字段名「目标层级”属旧机制术语残留 | 改「证据层级」或「所属层级」 |
| 12 | 评审中心空态 | 「添加目标层级证据后…」术语残留 | 改「添加下一级证据后…」 |
| 13 | 晋级后 | 「计划 09.30 前完成」沿用晋级前旧日期，未提示更新为新一级的计划日期 | 晋级通过后引导更新进展/重置计划日期 |

### 环境噪音（非应用问题）

- Console 报错 `Identifier 'ai_ide_policy_obj' has already been declared`：来自 IDE 浏览器扩展注入脚本，与应用无关。
- dev server 在长会话中曾两次自行退出（Turbopack dev 稳定性），生产构建不受影响，本地开发注意。

---

## 三、测试执行明细

1. 游客态检查 → 记录问题 5
2. 登录页检查 → 记录问题 9
3. 周宁登录：添加 L4 证据 ×3（每条后 hero/清单实时 +1）→ CTA 出现 → 选陈墨提交 → 「已提交，等待评审」成功页 → 状态锁定
4. 陈墨登录：hero 显示自身「评审中但 0/3」（问题 3/4）→ 评审中心待评 1 → 详情核对 3 条证据与标准映射 → 结论「已通过」+ 反馈 → 待评清零
5. 周宁复登：确认自动爬坡 L4→下一级 L5、反馈回显（核心闭环通过）
6. 林晓登录：团队分析数据实时性（晋级 1、L4=3 人）→ 晋级名单下钻档案 → 成果库（发现问题 1）→ 管理设置两个 tab（发现问题 10）→ 评审中心全部评审（发现问题 2、8）

---

## 四、修复记录（2026-07-25）

| # | 问题 | 修复方式 | 验证 |
|---|---|---|---|
| 1 | 「审核中」成果卡死 | 前端按钮/批量勾选/待审核统计均兼容「审核中」；seed 改为「待审核」 | 浏览器实机：王璐成果出现发布/退回按钮，点发布成功，计数联动 |
| 2 | 跨级在途申请 | reviewDecision 逐级防御：通过时 `promotedLevel = min(target, from+1)`，level_history 同步 | tsc 通过；存量 L4→L6 申请即使通过也只升到 L5 |
| 3 | seed 数据矛盾 | 为王璐(L6)、赵凯(L8) 补齐在途申请对应层级证据 | seed.ts 已更新（重新 seed 后生效） |
| 4 | 评审中仍提示缺证据 | 清单底部分支重排：pendingReviewId 优先 | 浏览器：林晓 0/3 且在途申请，正确显示「晋级申请评审中」 |
| 5 | 游客首页矛盾 | 摘要改「团队中位 Lx」；hero 改「体系起点 L1 · 登录后开始逐层爬坡」；进度点亮仅限登录态 | 浏览器：游客态文案与点亮均正确 |
| 6 | 退出无确认 | handleSignOut 增加 confirm | 浏览器：弹出确认框，接受后才退出 |
| 7 | 假步骤条 | 删除 step-indicator JSX/CSS 与 dialogStep 状态 | tsc 通过，弹窗无步骤条 |
| 8 | 评审动态下钻口径 | 点击时按角色设 scope：admin→全部，评审人→待评，成员→我的申请 | 代码已改 |
| 9 | 登录入口文案 | 「管理员密码登录」→「账号密码登录」 | 浏览器确认 |
| 10 | 「3 步引导」 | 改「2 步引导」 | 代码已改 |
| 11 | 证据弹窗「目标层级」 | 改「证据层级」 | 代码已改 |
| 12 | 评审空态术语 | 此前机制改造中已改为「添加下一级证据后…」 | 已确认 |
| 13 | 晋级后旧计划日期 | reviewStatus=已通过时 hero 显示「已晋级，请更新进展设定新一级计划」 | 代码已改 |

变更文件：`app/Dashboard.tsx`、`app/api/workspace/route.ts`、`app/login/page.tsx`、`app/globals.css`、`scripts/seed.ts`；tsc 退出码 0，页面 HTTP 200。
